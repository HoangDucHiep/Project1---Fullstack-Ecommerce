{
  "name": "@source-code/api-gateway",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@source-code/api-gateway:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@source-code/api-gateway:build:development"
          },
          "production": {
            "buildTarget": "@source-code/api-gateway:build:production"
          }
        }
      }
    }
  }
}
